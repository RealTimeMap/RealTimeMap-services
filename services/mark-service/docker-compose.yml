services:
  postgres:
    image: postgis/postgis:16-3.4-alpine
    container_name: postgis
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal-mark

  mark-service:
    build:
      context: ../..
      dockerfile: services/mark-service/Dockerfile
    container_name: mark-service
    depends_on:
      - postgres
    labels:
      - "traefik.enable=true"

      # HTTP API Router
      - "traefik.http.routers.mark.rule=PathPrefix(`/api/v2/`)"
      - "traefik.http.routers.mark.entrypoints=web"
      - "traefik.http.routers.mark.priority=1"

      # Socket.IO Router (higher priority)
      - "traefik.http.routers.mark-socketio.rule=PathPrefix(`/socket.io`)"
      - "traefik.http.routers.mark-socketio.entrypoints=web"
      - "traefik.http.routers.mark-socketio.priority=10"

      # Swagger Router
      - "traefik.http.routers.mark-swagger.rule=PathPrefix(`/swagger`)"
      - "traefik.http.routers.mark-swagger.entrypoints=web"
      - "traefik.http.routers.mark-swagger.priority=5"

      # Static files Router (store)
      - "traefik.http.routers.mark-store.rule=PathPrefix(`/store`)"
      - "traefik.http.routers.mark-store.entrypoints=web"
      - "traefik.http.routers.mark-store.priority=5"

      # Service configuration
      - "traefik.http.services.mark.loadbalancer.server.port=8080"
      - "traefik.http.services.mark.loadbalancer.sticky.cookie=true"
      - "traefik.http.services.mark.loadbalancer.sticky.cookie.name=mark_session"
    networks:
      - web
      - internal-mark

volumes:
  postgres_data:

networks:
  web:
    external: true
  internal-mark: